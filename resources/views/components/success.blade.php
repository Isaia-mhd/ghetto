@if (session()->has('success'))
    <div class="w-full bg-green-100 text-green-600 font-light text-sm px-3 py-2 rounded-sm flex justify-between my-2">
        <span>Propriété supprimé avec succès</span>
    </div>
@endif
